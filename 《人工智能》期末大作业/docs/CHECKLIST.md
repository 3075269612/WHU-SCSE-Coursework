# 提交前检查清单

- [ ] 已使用 `D:\Python\python.exe scripts\check_environment.py` 检查环境。
- [ ] 已运行 `scripts\download_data.py`，并确认 `data/processed/sms_spam.csv` 存在。
- [ ] 已运行 `run_experiments.py`，并确认 `results/` 中存在真实实验结果。
- [ ] 已确认训练/验证/测试划分约为 60%/20%/20%，且使用 `stratify=y`。
- [ ] 已确认 TF-IDF 只在训练集 fit，验证集和测试集只 transform。
- [ ] 已生成训练曲线、混淆矩阵、学习率敏感性图、Dropout 敏感性图。
- [ ] 已检查 baseline 对比，包括多数类、随机预测和 Logistic Regression。
- [ ] 已运行 `scripts\generate_report.py` 生成可编辑 Word 报告。
- [ ] 已运行 `scripts\check_project.py` 做提交前自动检查。
- [ ] 已阅读并理解代码，不提交未经理解的内容。
- [ ] 已用自己的话改写报告中的【建议用自己的话重写】段落。
- [ ] 已核对报告中的实验数值均来自 `results/` 文件。
- [ ] 已补充或确认个人分析，不保留不合适的【运行后填写】标记。
- [ ] 已向教师确认 PDF 中截止年份矛盾。
- [ ] Word 报告确认无乱码、图片缺失、表格错位后，再自行转换为 PDF 提交。
